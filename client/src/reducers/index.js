import * as actions from '../actions';

const initialState = {
  loading: false,
  error: null,
  questions: [],
  currentUser: null,
  accessToken: null,
};

const reducer = (state = initialState, action) => {

};

export default reducer;
